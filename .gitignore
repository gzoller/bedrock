.DS_Store
*.class
*.log
*~

# sbt specific
dist/*
target/
lib_managed/
src_managed/
project/boot/
project/plugins/project/
project/local-plugins.sbt
.history
.bsp
.metals
.bloop

# Scala-IDE specific
.scala_dependencies
.cache
.classpath
.project
.settings
classes/

# idea
.idea
.idea_modules
/.worksheet/

# Dotty-IDE
.dotty-ide-artifact
.dotty-ide.json

# Visual Studio Code
.vscode

# localstack
localstack-data
volume/

# certs
certs/

# Terraform state files (contain sensitive info)
terraform.tfstate
terraform.tfstate.backup

# Terraform working directory (caches provider plugins, local metadata)
.terraform/

# Terraform lock file (machine-specific, can be safely ignored in most cases)
.terraform.lock.hcl

# Terraform variable files (optional, if they contain secrets)
*.tfvars
*.tfvars.json

# Crash logs (debugging information for Terraform)
crash.log

# Ignore Terraform execution plan output (if manually saved)
*.tfplan

